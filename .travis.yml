language: swift
osx_image: xcode11.1

branches:
    only:
    - master

script:
  - set -o pipefail && xcodebuild test -project Macaw.xcodeproj -scheme 'Macaw' -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO -destination 'platform=iOS Simulator,OS=12.2,name=iPhone X' | xcpretty;
  - set -o pipefail && xcodebuild test -project Macaw.xcodeproj -scheme 'Macaw' ONLY_ACTIVE_ARCH=NO | xcpretty;


notifications:
    slack: exyte:kdo9FNtTOFMuMqAyyvZPDAD7
